# 🚀 快速开始指南

## 项目结构

完整的组件和代码示例已经创建在以下位置：

1. **架构设计文档**
   - `docs/ARCHITECTURE.md` - 完整的架构设计、数据模型、API 接口规范

2. **代码示例**  
   - `docs/code-samples/MainLayout.tsx` - 主布局组件
   - `docs/code-samples/TopBar.tsx` - 顶部栏组件
   - 其他组件示例请参考架构文档

## 本地启动命令

```bash
# 1. 安装依赖
npm install

# 2. 启动开发服务器
npm run dev

# 3. 构建生产版本
npm run build

# 4. 打包为桌面应用
npm run package
```

## 核心技术栈

- **前端框架**: React 18 + TypeScript
- **构建工具**: Vite
- **桌面容器**: Electron
- **状态管理**: Zustand
- **UI 组件**: Tailwind CSS + shadcn/ui
- **图标库**: Lucide React
- **路由**: React Router

## 下一步

1. 查看 `docs/ARCHITECTURE.md` 了解完整架构
2. 参考 `docs/code-samples/` 中的组件示例
3. 实现后台 IPC 通信接口
4. 连接 Python 后端服务

## 关键文件位置

```
src/
├── renderer/
│   ├── App.tsx              # 应用入口
│   ├── layouts/             # 布局组件
│   ├── views/              # 视图页面
│   ├── components/         # 通用组件
│   ├── types/              # TypeScript 类型
│   ├── services/           # API 服务层
│   └── store/              # 状态管理
└── main/                   # Electron 主进程
    └── ipc/                # IPC 处理器
```

详细实现请参考架构文档！
